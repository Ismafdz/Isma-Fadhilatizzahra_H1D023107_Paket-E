#include iostream
using namespace std;

bool isPrime(int num){
    if(num <=1) return false;
    for(int i=2; i*i<=num; i++){
        if(num%i==0) return false;
    }
    return true;
}

int stack(){
    int data=x;
}

void push(){
push=stack->data;
}

int count(){
data=x;
count=x;
}

int main(){
    alokasi(data(1));
    push &a;
    alokasi(data(3));
    push &a;
    alokasi(data(5));
    push &a;
    alokasi(data(7));
    push &a;
    alokasi(data(9));
    push &a;
    alokasi(data(11));
    push &a;
    alokasi(data(13));
    push &a;
    count=0;
}

return 0;
}